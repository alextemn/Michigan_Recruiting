# Optional: override API URL for local development.
# Leave unset to use production API (Heroku).
# Vercel: set VITE_API_URL in Project Settings → Environment Variables to the same value if you use a different backend.
VITE_API_URL=http://127.0.0.1:8000/api
